import { Title } from "@solidjs/meta";

export default function About() {
  return (
    <main>
      <Title>About - SolidStart + LAPLACE Event Bridge</Title>
      <h1>About</h1>
      <p>This is a demo application showing how to use SolidStart with LAPLACE Event Bridge.</p>
      <p>It connects to a WebSocket server to display live events from LAPLACE.live.</p>
    </main>
  );
}
