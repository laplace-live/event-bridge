{
  "name": "@laplace.live/event-bridge",
  "description": "LAPLACE Event Bridge",
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --hot run index.ts"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "^3.5.3"
  }
}
